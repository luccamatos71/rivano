---
import BaseLayout from "../layouts/BaseLayout.astro";
import { formatPriceBRL, toCardProduct, type Product } from "../lib/products";
import productsData from "../data/products.json";

const allProducts = productsData as Product[];
const editorialProducts = allProducts.slice(0, 4).map(toCardProduct);
const summerSelectionSlugs = ["rivano-saint-tropez", "rivano-tropea", "rivano-mykonos", "rivano-positano"] as const;
const summerSelectionProducts = summerSelectionSlugs.map((slug) => {
  const product = allProducts.find((item) => item.slug === slug);

  if (!product) {
    throw new Error(`Produto ${slug} não encontrado em products.json`);
  }

  return product;
});
const ibizaProduct = allProducts.find((product) => product.slug === "rivano-ibiza");

if (!ibizaProduct) {
  throw new Error("Produto Rivano Ibiza não encontrado em products.json");
}

const ibizaProductHref = `/produtos/${ibizaProduct.slug}`;
const ibizaProductName = ibizaProduct.name.replace(/^Rivano\s+/i, "");

---

<BaseLayout
  title="Rivano | Óculos premium para dias de sol"
  description="Óculos premium-acessíveis da Rivano. Design autoral, verão europeu e compra pelo WhatsApp."
  showAnnouncementBar={false}
  showBottomBar={false}
>
  <div class="rivano-home">
    <section class="campaign-hero" aria-label="Campanha Rivano verão 2025">
    <img
      class="campaign-motion-fallback"
      src="/banners/hero-mobile-fallback.webp"
      alt=""
      aria-hidden="true"
      loading="eager"
      fetchpriority="high"
      decoding="async"
    />
      <video
        class="campaign-image"
        data-hero-video
        autoplay
        muted
        defaultMuted
    loop
    playsinline
    webkit-playsinline="true"
    disablepictureinpicture
        preload="auto"
        poster="/banners/hero-mobile-poster.webp"
        width="720"
        height="1364"
        aria-label="Mulher usando óculos Rivano"
      >
        <source src="/banners/hero-mobile.mp4" type="video/mp4" />
      </video>
      <div class="campaign-gradient" aria-hidden="true"></div>
      <div class="campaign-content opening-entrance">
        <h1 class="campaign-title">Verão no olhar.</h1>
        <div class="campaign-actions" aria-label="Ações da campanha">
          <a class="campaign-cta" href="/catalogo">Ver modelos</a>
        </div>
      </div>
    </section>

    <section class="editorial-products opening-products" aria-labelledby="selecao-rivano">
      <div class="editorial-products-header opening-products-header">
        <p class="selection-label">Rivano eyewear</p>
        <h2 id="selecao-rivano" class="sr-only">Modelos em destaque</h2>
      </div>
      <div class="editorial-products-track opening-products-track" aria-label="Modelos em destaque">
        {
          editorialProducts.map((product, index) => (
            <a
              class="editorial-product"
              data-product={product.slug}
              href={`/produtos/${product.slug}`}
              aria-label={`Ver ${product.name}`}
            >
              <span class="editorial-product-frame">
                <img
                  class="editorial-product-image"
                  src={product.image}
                  alt={product.name}
                  loading={index === 0 ? "eager" : "lazy"}
                  decoding="async"
                />
              </span>
              <span class="editorial-product-name">{product.name.replace(/^Rivano\s+/i, "")}</span>
              <span class="editorial-product-price">{formatPriceBRL(product.price)}</span>
            </a>
          ))
        }
      </div>
      <div class="editorial-products-link-wrap">
        <a class="editorial-text-link" href="/catalogo">Todos os modelos</a>
      </div>
    </section>

    <section class="featured-product-story" aria-label="Campanha do modelo Ibiza">
      <div class="featured-product-story-stage">
        <picture>
          <source media="(min-width: 1024px)" srcset="/editorial/rivano-ibiza-hero-wide.webp" width="2480" height="1472" />
          <img
            class="featured-product-story-image"
            src="/editorial/rivano-ibiza-hero-portrait.webp"
            alt="Modelo Rivano usando os óculos Ibiza em uma paisagem de montanha com neve"
            width="1646"
            height="3480"
            loading="lazy"
            decoding="async"
          />
        </picture>


        <div class="featured-product-commerce" aria-label={`Informações do modelo ${ibizaProductName}`}>
          <div class="featured-product-commerce-details">
            <div>
              <p class="featured-product-commerce-name">{ibizaProductName}</p>
              <p class="featured-product-commerce-price">{formatPriceBRL(ibizaProduct.price)}</p>
            </div>
            <a class="featured-product-commerce-link" href={ibizaProductHref}>
              <span>Ver modelo</span>
              <svg viewBox="0 0 18 8" aria-hidden="true">
                <path d="M0 4h16M12.5.5 16 4l-3.5 3.5" fill="none" stroke="currentColor" stroke-linecap="square" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </section>

    <section class="rivano-travel-manifesto" aria-labelledby="rivano-travel-manifesto-title">
      <h2 id="rivano-travel-manifesto-title" class="sr-only">Em algum lugar sob o sol</h2>
      <p class="rivano-travel-manifesto-copy">
        <span class="rivano-travel-manifesto-line--ibiza">Ibiza at 6pm.</span>
        <span class="rivano-travel-manifesto-line--capri">Capri after <em>lunch.</em></span>
        <span class="rivano-travel-manifesto-line--boat">The last boat.</span>
        <span class="rivano-travel-manifesto-line--mykonos">Mykonos until</span>
        <em class="rivano-travel-manifesto-line--sunset">sunset.</em>
        <span class="rivano-travel-manifesto-line--dinner">Dinner at ten.</span>
        <span class="rivano-travel-manifesto-line--saint">Saint-Tropez</span>
        <span class="rivano-travel-manifesto-line--dark">after dark.</span>
        <span class="rivano-travel-manifesto-line--swim">One more <em>swim.</em></span>
        <span class="rivano-travel-manifesto-line--somewhere">Somewhere</span>
        <em class="rivano-travel-manifesto-line--under">under the sun.</em>
      </p>
    </section>

    <section class="rivano-summer-notes" aria-label="Rivano Summer Notes">
      <div class="rivano-summer-notes-stage">
        <img
          src="/editorial/rivano-summer-notes-final.webp"
          alt="Arte editorial Último barco, com óculos Rivano, laranja sanguínea e copo âmbar"
          class="rivano-summer-notes-image"
          width="1122"
          height="1402"
          loading="lazy"
          decoding="async"
        />

        <figure class="rivano-summer-notes-inset" aria-hidden="true">
          <img
            src="/editorial/rivano-summer-notes.webp"
            alt=""
            width="1122"
            height="1402"
            loading="lazy"
            decoding="async"
          />
        </figure>

      </div>
    </section>

    <section class="summer-selection" aria-labelledby="summer-selection-title">
      <header class="summer-selection-header">
        <h2 id="summer-selection-title">Seleção de verão</h2>
      </header>
      <div class="summer-selection-grid">
        {
          summerSelectionProducts.map((product) => {
            const modelName = product.name.replace(/^Rivano\s+/i, "").replaceAll("-", " ");

            return (
              <a class="summer-selection-product" href={`/produtos/${product.slug}`} aria-label={`Ver ${product.name}`}>
                <span class="summer-selection-product-stage">
                  <img
                    src={`/products/${product.slug}/editorial-grid.webp`}
                    alt={product.name}
                    loading="lazy"
                    decoding="async"
                  />
                </span>
                <span class="summer-selection-product-name">{modelName}</span>
                <span class="summer-selection-product-price">{formatPriceBRL(product.price)}</span>
              </a>
            );
          })
        }
      </div>
    </section>

    <section class="rivano-fits" aria-labelledby="rivano-fits-title">
      <div class="rivano-fits-intro">
        <p class="rivano-fits-eyebrow">Rivano Society</p>
        <h2 id="rivano-fits-title" class="rivano-fits-title"><span class="rivano-fits-phrase">Seen on you.</span> <span class="rivano-fits-phrase">Somewhere under the sun.</span></h2>
      </div>

      <div class="rivano-fits-duo">
        <figure class="rivano-fits-cell">
          <img
            class="rivano-fits-photo"
            src="/editorial/lifestyle/rivano-lifestyle-05.webp"
            alt="Óculos Rivano sob a sombra da folhagem"
            width="1080"
            height="1350"
            loading="lazy"
            decoding="async"
          />
        </figure>
        <figure class="rivano-fits-cell">
          <img
            class="rivano-fits-photo rivano-fits-photo--sea"
            src="/editorial/lifestyle/rivano-lifestyle-01.webp"
            alt="Óculos Rivano redondos em passeio de barco"
            width="1080"
            height="1350"
            loading="lazy"
            decoding="async"
          />
        </figure>
      </div>

      <div class="rivano-fits-mosaic">
        <figure class="rivano-fits-cell rivano-fits-cell--dominant">
          <img
            class="rivano-fits-photo rivano-fits-photo--dominant"
            src="/editorial/lifestyle/rivano-lifestyle-02-tall.webp"
            alt="Look de verão com óculos Rivano entre palmeiras"
            width="1000"
            height="1500"
            loading="lazy"
            decoding="async"
          />
        </figure>
        <figure class="rivano-fits-cell">
          <img
            class="rivano-fits-photo"
            src="/editorial/lifestyle/rivano-lifestyle-10.webp"
            alt="Óculos Rivano brancos apoiados sobre uma bolsa"
            width="1080"
            height="1350"
            loading="lazy"
            decoding="async"
          />
        </figure>
        <figure class="rivano-fits-cell">
          <img
            class="rivano-fits-photo rivano-fits-photo--portrait"
            src="/editorial/lifestyle/rivano-lifestyle-04.webp"
            alt="Retrato com óculos Rivano de lente âmbar"
            width="1080"
            height="1350"
            loading="lazy"
            decoding="async"
          />
        </figure>
        <figure class="rivano-fits-cell rivano-fits-cell--wide-only">
          <img
            class="rivano-fits-photo"
            src="/editorial/lifestyle/rivano-lifestyle-03.webp"
            alt="Óculos Rivano brancos no fim de tarde na marina"
            width="1080"
            height="1350"
            loading="lazy"
            decoding="async"
          />
        </figure>
        <figure class="rivano-fits-cell rivano-fits-cell--wide-only">
          <img
            class="rivano-fits-photo"
            src="/editorial/lifestyle/rivano-lifestyle-06.webp"
            alt="Óculos Rivano no fim de tarde na rua"
            width="1080"
            height="1350"
            loading="lazy"
            decoding="async"
          />
        </figure>
      </div>

      <div class="rivano-fits-outro">
        <h3 class="rivano-fits-outro-title">O verão tem seus lugares. Este é um deles.</h3>
        <p class="rivano-fits-outro-text">Feito para os lugares que você ainda vai conhecer e para aqueles aos quais sempre acaba voltando.</p>
        <a class="rivano-fits-outro-link" href="/catalogo">
          <svg class="rivano-fits-outro-marker" viewBox="0 0 24 24" width="12" height="16" aria-hidden="true" focusable="false">
            <path fill="currentColor" d="m17 12-7.5 4.5v-9z" />
          </svg>
          Ver todos os modelos
        </a>
      </div>

      <figure class="rivano-break">
        <picture>
          <source media="(min-width: 768px)" srcset="/editorial/lifestyle/rivano-break-wide.webp" width="1024" height="576" />
          <img
            class="rivano-break-photo"
            src="/editorial/lifestyle/rivano-break-portrait.webp"
            alt="Óculos Rivano brancos sobre uma revista, ao lado de bolsa de palha e chapéu na areia"
            width="1024"
            height="1365"
            loading="lazy"
            decoding="async"
          />
        </picture>
      </figure>
    </section>

  </div>
</BaseLayout>

<script is:inline>
  const heroVideo = document.querySelector("[data-hero-video]");
  const campaignHero = document.querySelector(".campaign-hero");
  const openingProducts = document.querySelector(".opening-products");
  const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");

  const startHeroVideo = () => {
    if (!(heroVideo instanceof HTMLVideoElement)) return;

    heroVideo.muted = true;
    heroVideo.defaultMuted = true;
    heroVideo.setAttribute("muted", "");
    heroVideo.play().catch(() => undefined);
  };

  const revealHeroVideo = () => campaignHero?.classList.add("is-video-playing");

  heroVideo?.addEventListener("playing", revealHeroVideo);
  startHeroVideo();
  ["loadedmetadata", "loadeddata", "canplay", "playing"].forEach((eventName) => {
    heroVideo?.addEventListener(eventName, startHeroVideo, { once: true });
  });
  document.addEventListener("visibilitychange", () => {
    if (document.visibilityState === "visible") startHeroVideo();
  });
  if (heroVideo instanceof HTMLVideoElement && !heroVideo.paused && heroVideo.readyState >= 2) {
    revealHeroVideo();
  }

  if (openingProducts) {
    const revealProducts = () => openingProducts.classList.add("is-revealed");

    if (reducedMotion.matches || !("IntersectionObserver" in window)) {
      revealProducts();
    } else {
      const openingProductsObserver = new IntersectionObserver(
        ([entry]) => {
          if (!entry.isIntersecting) return;
          revealProducts();
          openingProductsObserver.disconnect();
        },
        { threshold: 0.18 },
      );

      openingProductsObserver.observe(openingProducts);
    }
  }

</script>

<style>
  .rivano-home {
    background: #fff;
    color: var(--rivano-brown);
    --summer-notes-inset-left: 4.2vw;
    overflow-x: clip;
  }

  .campaign-hero {
    background: #e5e8e9;
    height: 100svh;
    min-height: 640px;
    overflow: hidden;
    position: relative;
    width: 100vw;
  }

  .campaign-motion-fallback,
  .campaign-image {
    background: #e5e8e9;
    display: block;
    height: 100%;
    left: 0;
    object-fit: cover;
    object-position: center;
    position: absolute;
    top: 0;
    width: 100vw;
  }

  .campaign-motion-fallback {
    object-fit: cover;
    object-position: center;
    z-index: 0;
  }

  .campaign-image {
    opacity: 0;
    transition: opacity 180ms ease-out;
    z-index: 1;
  }

  .campaign-hero.is-video-playing .campaign-image {
    opacity: 1;
  }

  .campaign-gradient {
    background: linear-gradient(0deg, rgba(9, 12, 13, 0.72) 0%, rgba(9, 12, 13, 0.2) 36%, transparent 64%);
    bottom: 0;
    height: 52%;
    left: 0;
    pointer-events: none;
    position: absolute;
    right: 0;
    z-index: 2;
  }

  .campaign-content {
    bottom: 0;
    left: 0;
    padding: 24px 20px calc(32px + env(safe-area-inset-bottom));
    position: absolute;
    width: 100%;
    z-index: 3;
  }

  .selection-label {
    font-family: var(--font-sans);
    font-weight: 400;
    text-transform: uppercase;
  }

  .campaign-title {
    color: var(--rivano-off-white);
    font-family: var(--font-sans);
    font-size: clamp(46px, 12.4vw, 58px);
    font-weight: 400;
    letter-spacing: -0.065em;
    line-height: 0.88;
    margin: 0;
    max-width: 260px;
  }

  .campaign-actions {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    margin-top: 22px;
  }

  .campaign-cta {
    align-items: center;
    color: var(--rivano-off-white);
    display: inline-flex;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 400;
    border-bottom: 1px solid color-mix(in srgb, var(--rivano-off-white) 82%, transparent);
    letter-spacing: 0.1em;
    line-height: 1.2;
    min-height: 40px;
    padding-bottom: 2px;
    text-transform: uppercase;
  }

  .featured-product-story {
    background: #fff;
    padding-bottom: 56px;
    padding-top: 20px;
    position: relative;
  }

  .featured-product-story-stage {
    background: #fff;
    height: clamp(680px, 94svh, 860px);
    isolation: isolate;
    overflow: hidden;
    position: relative;
    touch-action: pan-y;
    user-select: none;
    width: 100%;
  }

  .featured-product-story-stage picture {
    display: block;
    inset: 0;
    position: absolute;
  }

  .featured-product-story-image {
    display: block;
    height: 100%;
    object-fit: cover;
    object-position: 50% 32%;
    width: 100%;
  }

  .featured-product-commerce-link:focus-visible {
    outline: 1px solid #1d211f;
    outline-offset: 4px;
  }

  .featured-product-commerce {
    align-items: stretch;
    background: #f3efe5;
    border-top: 1px solid rgba(29, 33, 31, 0.66);
    bottom: 0;
    box-sizing: border-box;
    color: #1d211f;
    display: grid;
    grid-template-columns: 1fr;
    left: 0;
    min-height: calc(84px + env(safe-area-inset-bottom));
    padding: 18px 18px calc(16px + env(safe-area-inset-bottom));
    position: absolute;
    right: 0;
    width: auto;
    z-index: 6;
  }

  .featured-product-commerce-details {
    align-items: end;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0;
  }

  .featured-product-commerce-name,
  .featured-product-commerce-price {
    font-family: var(--font-sans);
    margin: 0;
  }

  .featured-product-commerce-name {
    font-size: 16px;
    font-weight: 500;
    letter-spacing: -0.035em;
    line-height: 1;
    text-transform: uppercase;
  }

  .featured-product-commerce-price {
    color: rgba(29, 33, 31, 0.72);
    font-size: 10px;
    line-height: 1.3;
    margin-top: 3px;
  }

  .featured-product-commerce-link {
    align-items: center;
    border-bottom: 0;
    border-left: 1px solid rgba(29, 33, 31, 0.44);
    color: #1d211f;
    display: inline-flex;
    font-family: var(--font-sans);
    font-size: 9px;
    font-weight: 500;
    gap: 8px;
    letter-spacing: 0.09em;
    line-height: 1.2;
    align-self: end;
    margin: 0;
    min-height: 42px;
    padding: 0 0 0 20px;
    text-transform: uppercase;
  }

  .featured-product-commerce-link span {
    border-bottom: 1px solid currentColor;
    padding-bottom: 3px;
  }

  .featured-product-commerce-link svg {
    height: 8px;
    width: 18px;
  }


  .opening-products {
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.16) 0, #fff 88px);
    margin-top: -42px;
    padding: 74px 0 56px;
    position: relative;
    z-index: 1;
  }

  .opening-products-header {
    margin-bottom: 14px;
    padding: 0 16px;
  }

  .selection-label {
    color: color-mix(in srgb, var(--rivano-brown) 62%, transparent);
    font-size: 9px;
    letter-spacing: 0.15em;
    line-height: 1.35;
  }

  .selection-title {
    color: var(--rivano-brown);
    font-family: var(--font-serif);
    font-size: clamp(38px, 11vw, 56px);
    font-weight: 300;
    letter-spacing: 0;
    line-height: 0.92;
    margin-top: 10px;
    max-width: 290px;
  }

  .opening-products-track {
    display: grid;
    gap: 22px 10px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    padding: 0 16px;
  }

  .editorial-product {
    color: var(--rivano-brown);
    margin-top: 0;
    min-height: 44px;
    min-width: 0;
  }

  .editorial-product-frame {
    align-items: center;
    aspect-ratio: 4 / 3;
    background: transparent;
    display: flex;
    justify-content: center;
    overflow: hidden;
    padding: 0;
    width: 100%;
  }

  .editorial-product-image {
    display: block;
    filter: contrast(1.04) saturate(0.98);
    height: 100%;
    mix-blend-mode: multiply;
    object-fit: cover;
    width: 100%;
  }

  .editorial-product[data-product="rivano-balos"] .editorial-product-image {
    object-position: center 52%;
    transform: scale(1.1);
  }

  .editorial-product[data-product="rivano-amalfi"] .editorial-product-image {
    object-position: center 51%;
    transform: scale(1.03);
  }

  .editorial-product[data-product="rivano-palermo"] .editorial-product-image {
    object-position: center 51%;
    transform: scale(1.05);
  }

  .editorial-product[data-product="rivano-verona1"] .editorial-product-image {
    object-position: center 50%;
    transform: scale(0.98);
  }

  .editorial-product-name {
    display: block;
    font-family: var(--font-sans);
    font-size: 12px;
    font-weight: 400;
    letter-spacing: -0.02em;
    line-height: 1.35;
    margin-top: 9px;
  }

  .editorial-product-price {
    color: color-mix(in srgb, var(--rivano-brown) 56%, transparent);
    display: block;
    font-family: var(--font-sans);
    font-size: 12px;
    font-weight: 300;
    line-height: 1.35;
    margin-top: 3px;
  }

  .editorial-products-link-wrap {
    margin: 24px 16px 0;
  }

  .editorial-text-link {
    align-items: center;
    border-bottom: 1px solid currentColor;
    color: var(--rivano-brown);
    display: inline-flex;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 0.14em;
    line-height: 1.2;
    min-height: 44px;
    text-transform: uppercase;
  }

  .opening-entrance {
    animation: opening-copy-in 560ms 80ms both cubic-bezier(0.16, 1, 0.3, 1);
  }

  .opening-products .editorial-product {
    opacity: 0;
    transform: translateY(14px);
  }

  .opening-products.is-revealed .editorial-product {
    animation: opening-product-in 520ms both cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes opening-copy-in {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes opening-product-in {
    from { opacity: 0; transform: translateY(14px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .rivano-travel-manifesto {
    background: #fff;
    color: #171816;
    overflow: hidden;
    padding: clamp(52px, 7svh, 70px) 14px calc(clamp(58px, 8svh, 82px) - 16px);
  }

  .rivano-travel-manifesto-copy {
    display: flex;
    flex-direction: column;
    font-family: var(--font-sans);
    font-size: clamp(43px, 11.3vw, 52px);
    font-style: normal;
    font-weight: 400;
    letter-spacing: -0.075em;
    line-height: 0.82;
    margin: 0;
    text-transform: uppercase;
  }

  .rivano-travel-manifesto-copy > span,
  .rivano-travel-manifesto-copy > em {
    display: block;
    max-width: 100%;
    white-space: nowrap;
  }

  .rivano-travel-manifesto-copy em {
    font-family: var(--font-serif);
    font-size: 1.08em;
    font-style: italic;
    font-weight: 300;
    letter-spacing: -0.055em;
    line-height: 0.8;
    text-transform: none;
  }

  .rivano-travel-manifesto-line--capri {
    font-size: 0.94em;
    margin-left: 8.5vw;
  }

  .rivano-travel-manifesto-line--capri em,
  .rivano-travel-manifesto-line--swim em {
    font-size: 1.14em;
    margin-left: 3px;
  }

  .rivano-travel-manifesto-line--boat {
    font-size: 0.79em;
    letter-spacing: -0.055em;
    margin-left: 8%;
    margin-top: -2px;
  }

  .rivano-travel-manifesto-line--mykonos {
    margin-top: 3px;
  }

  .rivano-travel-manifesto-line--sunset {
    font-size: 1.18em;
    margin-left: 52%;
    margin-top: -3px;
  }

  .rivano-travel-manifesto-line--dinner {
    font-size: 0.76em;
    letter-spacing: -0.055em;
    margin-left: 38%;
    margin-top: 0;
  }

  .rivano-travel-manifesto-line--saint {
    font-size: 0.98em;
    margin-top: 3px;
  }

  .rivano-travel-manifesto-line--dark {
    font-size: 0.93em;
    margin-left: 35%;
    margin-top: -2px;
  }

  .rivano-travel-manifesto-line--swim {
    font-size: 0.82em;
    margin-left: 7%;
    margin-top: 0;
  }

  .rivano-travel-manifesto-line--somewhere {
    font-size: 0.95em;
    margin-left: 13%;
    margin-top: clamp(12px, 2svh, 18px);
  }

  .rivano-travel-manifesto-line--under {
    font-size: 1.2em;
    margin-left: 24%;
    margin-top: -5px;
  }

  .rivano-summer-notes {
    background: var(--rivano-off-white);
    padding: 0 0 clamp(86px, 15vw, 118px);
  }

  .rivano-summer-notes-stage {
    aspect-ratio: 0.8;
    isolation: isolate;
    overflow: visible;
    position: relative;
    width: 100%;
  }

  .rivano-summer-notes-image {
    display: block;
    height: 100%;
    object-fit: cover;
    object-position: 50% 52%;
    width: 100%;
  }

  .rivano-summer-notes-title {
    color: var(--rivano-brown);
    left: 0;
    position: absolute;
    top: 11.5%;
    width: 100%;
    z-index: 1;
  }

  .rivano-summer-notes-statement {
    font-family: var(--font-sans);
    font-size: clamp(32px, 8.6vw, 36px);
    font-weight: 400;
    letter-spacing: -0.075em;
    line-height: 0.8;
    margin: 0;
  }

  .rivano-summer-notes-statement span {
    display: block;
    width: fit-content;
  }

  .rivano-summer-notes-statement span:first-child {
    margin-left: 6.2vw;
  }

  .rivano-summer-notes-statement span:last-child {
    margin-left: 19vw;
    margin-top: 25px;
  }

  .rivano-summer-notes-intervention {
    font-family: var(--font-serif);
    font-size: clamp(20px, 5.2vw, 23px);
    font-style: italic;
    font-weight: 300;
    letter-spacing: -0.055em;
    line-height: 0.84;
    margin: 0;
    position: absolute;
    top: 30px;
    left: 10vw;
  }

  .rivano-summer-notes-inset {
    aspect-ratio: 3 / 4;
    bottom: clamp(-76px, -18vw, -62px);
    left: var(--summer-notes-inset-left);
    margin: 0;
    overflow: visible;
    position: absolute;
    width: clamp(116px, 32vw, 144px);
    z-index: 2;
  }

  .rivano-summer-notes-inset img {
    display: block;
    height: 100%;
    object-fit: cover;
    object-position: 65% 60%;
    width: 100%;
  }

  .rivano-summer-notes-annotation {
    bottom: 11.5%;
    color: var(--rivano-off-white);
    display: flex;
    flex-direction: column;
    font-family: var(--font-sans);
    font-size: 9px;
    font-weight: 400;
    letter-spacing: 0.13em;
    line-height: 1.25;
    margin: 0;
    position: absolute;
    right: 7.4vw;
    text-align: right;
    text-transform: uppercase;
    z-index: 1;
  }

  .rivano-summer-notes-annotation span + span {
    margin-top: 3px;
  }

  .summer-selection {
    background: #fff;
    color: var(--rivano-brown);
    padding: 0 16px 96px;
  }

  .summer-selection-header {
    margin: 0 0 34px;
    padding-left: calc(var(--summer-notes-inset-left) - 16px);
  }

  .summer-selection-header h2 {
    font-family: var(--font-sans);
    font-size: 10px;
    font-weight: 400;
    letter-spacing: 0.16em;
    line-height: 1.25;
    margin: 0;
    text-transform: uppercase;
  }

  .summer-selection-grid {
    display: grid;
    gap: 60px 12px;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .summer-selection-product {
    color: inherit;
    display: block;
    min-width: 0;
    text-decoration: none;
  }

  .summer-selection-product-stage {
    align-items: center;
    aspect-ratio: 1 / 1;
    display: flex;
    justify-content: center;
    width: 100%;
  }

  .summer-selection-product-stage img {
    display: block;
    height: auto;
    max-height: 100%;
    object-fit: contain;
    width: 92%;
  }

  .summer-selection-product-name,
  .summer-selection-product-price {
    display: block;
    font-family: var(--font-sans);
  }

  .summer-selection-product-name {
    font-size: 10px;
    font-weight: 400;
    letter-spacing: 0.1em;
    line-height: 1.35;
    margin-top: 10px;
    text-transform: uppercase;
  }

  .summer-selection-product-price {
    color: color-mix(in srgb, var(--rivano-brown) 62%, transparent);
    font-size: 11px;
    font-weight: 300;
    line-height: 1.35;
    margin-top: 3px;
  }

  /* ── Rivano Society — clone estrutural do bloco entre "Summer in the
     city" e "Jacket in denim" na pagina Paulas Ibiza da loewe.com.
     Numeros medidos na implementacao real, em 390px e em 1440px:

       intro   texto com padding lateral
       DUO     duas celulas de 50%, proporcao 3:4, sem gutter
       MOSAIC  dominante de 2 unidades + menores de 1 unidade,
               proporcao 2:3, alinhadas ao topo, sem gutter — o branco
               abaixo das menores e intencional

     A faixa vale 4 unidades no mobile (dominante + 2 menores) e 6 no
     desktop (dominante + 4 menores), como na referencia. O peso da
     secao vem da escala da fotografia, nao de massa de cor. */
  .rivano-fits {
    background: #fff;
    color: var(--rivano-brown);
    padding: 64px 0 0;
  }

  .rivano-fits-intro {
    padding: 0 24px 28px;
  }

  /* Marcador editorial: mesmo vocabulario dos labels da home. */
  .rivano-fits-eyebrow {
    color: color-mix(in srgb, var(--rivano-brown) 45%, transparent);
    font-family: var(--font-sans);
    font-size: 10px;
    font-weight: 400;
    letter-spacing: 0.16em;
    line-height: 1.35;
    margin: 0;
    text-transform: uppercase;
  }

  /* Serif do sistema, como toda headline editorial da home
     serif do sistema, escala menor que a headline principal.
     A escala acompanha a viewport para manter cada frase inteira. */
  .rivano-fits-title {
    color: var(--rivano-brown);
    font-family: var(--font-serif);
    font-size: clamp(26px, 8vw, 40px);
    font-weight: 300;
    letter-spacing: 0;
    line-height: 1.05;
    margin: 12px 0 0;
  }

  /* Cada frase e uma unidade: a quebra cai ENTRE as frases, nunca
     dentro de "Somewhere under the sun.". */
  .rivano-fits-phrase {
    display: inline-block;
    white-space: nowrap;
  }

  @media (max-width: 339px) {
    .rivano-fits-phrase {
      white-space: normal;
    }
  }

  /* Bloco A — duas celulas de meia largura, coladas, edge-to-edge. */
  .rivano-fits-duo {
    display: grid;
    gap: 0;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .rivano-fits-duo .rivano-fits-cell {
    aspect-ratio: 3 / 4;
  }

  /* Bloco B — a dominante vale duas unidades e desce o dobro; as
     menores param no topo e deixam o branco embaixo. */
  .rivano-fits-mosaic {
    align-items: start;
    display: grid;
    gap: 0;
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .rivano-fits-mosaic .rivano-fits-cell {
    aspect-ratio: 2 / 3;
  }

  .rivano-fits-cell--dominant {
    grid-column: span 2;
  }

  .rivano-fits-cell--wide-only {
    display: none;
  }

  .rivano-fits-cell {
    margin: 0;
    overflow: hidden;
    position: relative;
  }

  .rivano-fits-photo {
    display: block;
    height: 100%;
    object-fit: cover;
    object-position: 50% 50%;
    width: 100%;
  }

  .rivano-fits-photo--dominant {
    object-position: 50% 50%;
  }

  .rivano-fits-photo--portrait {
    object-position: 50% 38%;
  }

  .rivano-fits-photo--sea {
    object-position: 50% 40%;
  }

  /* Bloco editorial de fechamento — clone do micro-layout "Woven
     baskets" da mesma pagina de referencia. Medidas reais em 390px:
     coluna de 176px (45,8vw) alinhada a esquerda, 59px de respiro
     depois da fotografia, titulo 18/22.5, 16px ate o paragrafo
     14/19.6, 16px ate o link, 48px ate o proximo capitulo.
     A coluna estreita e o branco em volta sao o desenho: nao encher. */
  .rivano-fits-outro {
    margin: 72px 24px 0;
    max-width: 82%;
  }

  .rivano-fits-outro-title {
    color: var(--rivano-brown);
    font-family: var(--font-serif);
    font-size: 18px;
    font-weight: 300;
    letter-spacing: 0.005em;
    line-height: 1.35;
    margin: 0 0 14px;
  }

  .rivano-fits-outro-text {
    color: color-mix(in srgb, var(--rivano-brown) 70%, transparent);
    font-family: var(--font-sans);
    font-size: 13px;
    font-weight: 300;
    line-height: 1.6;
    margin: 0;
  }

  /* Marcador triangular do link, como na referencia. */
  .rivano-fits-outro-link {
    align-items: center;
    color: var(--rivano-brown);
    display: inline-flex;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 400;
    gap: 4px;
    letter-spacing: 0.12em;
    line-height: 1.2;
    margin-top: 14px;
    min-height: 44px;
    text-decoration: none;
    text-transform: uppercase;
  }

  .rivano-fits-outro-marker {
    display: block;
    flex: none;
  }

  /* Quebra fotografica — clone da transicao "texto -> fotografia
     full-width" da mesma pagina de referencia. Medidas reais:
     48px de respiro depois do texto (constante em mobile e desktop),
     largura cheia sem margem lateral no mobile, proporcao 3:4;
     em >=768px a margem de 24px do container entra e a proporcao vira
     16:9, como a referencia faz nesse mesmo bloco.
     Fotografia pura: sem texto, overlay, borda ou raio. */
  .rivano-break {
    margin: 48px 0 0;
  }

  .rivano-break picture {
    display: block;
  }

  .rivano-break-photo {
    aspect-ratio: 3 / 4;
    display: block;
    height: auto;
    object-fit: cover;
    object-position: 50% 50%;
    width: 100%;
  }

  @media (min-width: 768px) {
    .campaign-hero {
      height: 100vh;
    }

    .campaign-content {
      padding: 40px;
    }

    .campaign-title {
      font-size: clamp(72px, 9vw, 128px);
      max-width: 560px;
    }

    .editorial-products {
      padding: 96px 40px;
    }

    .opening-products {
      background: linear-gradient(180deg, transparent 0, #fff 144px);
      margin-top: -112px;
      padding: 176px 40px 96px;
    }

    .editorial-products-header {
      margin: 0 auto 36px;
      max-width: 1280px;
      padding: 0;
    }

    .selection-title {
      max-width: 520px;
    }

    .editorial-products-track {
      display: grid;
      gap: 24px;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      margin: 0 auto;
      max-width: 1280px;
      overflow: visible;
      padding: 0;
      scroll-snap-type: none;
    }

    .opening-products .editorial-product {
      animation: none;
      margin-top: 0;
    }

    .opening-products .editorial-product-image {
      height: 100%;
      max-width: none;
    }

    .editorial-products-link-wrap {
      margin: 32px auto 0;
      max-width: 1280px;
    }

    .rivano-travel-manifesto {
      padding: 144px 40px 156px;
    }

    .rivano-travel-manifesto-copy {
      font-size: clamp(68px, 8.5vw, 132px);
      margin: 0 auto;
      max-width: 1280px;
    }

    .rivano-summer-notes {
      padding-bottom: 170px;
    }

    .rivano-summer-notes-stage {
      aspect-ratio: 1.55;
      margin: 0 auto;
      max-width: 1440px;
    }

    .rivano-summer-notes-title {
      left: 5%;
      top: 9%;
    }

    .rivano-summer-notes-statement {
      font-size: clamp(58px, 7vw, 104px);
    }

    .rivano-summer-notes-intervention {
      font-size: clamp(32px, 3.2vw, 48px);
      left: 10vw;
      top: clamp(48px, 5vw, 76px);
    }

    .rivano-summer-notes-inset {
      bottom: -112px;
      left: 5%;
      width: clamp(176px, 17vw, 250px);
    }

    .rivano-summer-notes-annotation {
      bottom: 10%;
      right: 5%;
    }

    .summer-selection {
      padding: 0 40px 144px;
    }

    .summer-selection-header,
    .summer-selection-grid {
      margin-left: auto;
      margin-right: auto;
      max-width: 1280px;
    }

    .summer-selection-grid {
      gap: 72px 28px;
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    /* ≥768: as margens laterais entram (24px na referencia) e a faixa
       passa a valer 6 unidades — a dominante cede largura e duas
       menores extras aparecem, como no desktop da referencia. */
    .rivano-fits {
      padding: 104px 0 24px;
    }

    /* No desktop a referencia mantem a mesma tipografia e apenas afasta
       o bloco da fotografia (139px) — a coluna segue estreita. */
    /* A medida cresce para a frase respirar; o bloco continua na
       esquerda e o branco a direita segue sendo o desenho. */
    .rivano-fits-outro {
      margin-top: 139px;
      max-width: 460px;
    }

    .rivano-fits-outro-title {
      font-size: 22px;
      margin-bottom: 16px;
    }

    .rivano-fits-outro-text {
      font-size: 14px;
    }

    .rivano-break {
      margin: 48px 24px 0;
    }

    .rivano-break-photo {
      aspect-ratio: 16 / 9;
    }

    .rivano-fits-intro {
      padding: 0 24px 36px;
    }

    .rivano-fits-title {
      font-size: clamp(40px, 4.4vw, 62px);
      margin-top: 14px;
    }

    .rivano-fits-duo,
    .rivano-fits-mosaic {
      margin: 0 24px;
    }

    .rivano-fits-mosaic {
      grid-template-columns: repeat(6, minmax(0, 1fr));
    }

    .rivano-fits-cell--wide-only {
      display: block;
    }

  }

  @media (prefers-reduced-motion: reduce) {
    .opening-entrance,
    .opening-products .editorial-product {
      animation: none;
      opacity: 1;
      transform: none;
    }

  }
</style>
